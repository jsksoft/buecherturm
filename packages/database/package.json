{
  "name": "@buecherturm/database",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "db:generate": "dotenv -e ../../apps/web/.env.local -- drizzle-kit generate",
    "db:push": "dotenv -e ../../apps/web/.env.local -- drizzle-kit push"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "dotenv-cli": "^8.0.0",
    "drizzle-kit": "^0.31.0",
    "typescript": "^5.8.3"
  }
}
